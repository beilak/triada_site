{
  "name": "company-llc-demo-site",
  "version": "1.0.0",
  "private": true,
  "description": "Simple SSR/static company site based on the TRIADA LLC reference layout.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "serve": "node scripts/serve.mjs",
    "start": "node scripts/start.mjs"
  },
  "engines": {
    "node": ">=18"
  }
}
